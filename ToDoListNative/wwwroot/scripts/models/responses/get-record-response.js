class GetRecordInfoResponse {
    constructor(id, title, content, isComplete) {
        this.id = id;
        this.title = title;
        this.content = content;
        this.isComplete = isComplete;
    }
}